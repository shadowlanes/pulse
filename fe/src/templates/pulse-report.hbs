<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pulse | {{dateStr}}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Instrument+Sans:wght@400;500;600;700&family=Outfit:wght@400;500;600;700;800;900&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --bg: #0a0a0a;
            --text: #ededed;
            --muted: #888888;

            --accent: {
                    {
                    #if isGood
                }
            }

            #22c55e {
                    {
                    else
                }
            }

            #ef4444 {
                    {
                    /if
                }
            }

            ;

            --accent-glow: {
                    {
                    #if isGood
                }
            }

            rgba(34, 197, 94, 0.2) {
                    {
                    else
                }
            }

            rgba(239, 68, 68, 0.2) {
                    {
                    /if
                }
            }

            ;
            --card: rgba(23, 23, 23, 0.6);
            --border: rgba(255, 255, 255, 0.1);
        }

        * {
            box-sizing: border-box;
        }

        body {
            background-color: var(--bg);
            color: var(--text);
            font-family: 'Instrument Sans', system-ui, -apple-system, sans-serif;
            line-height: 1.7;
            letter-spacing: -0.01em;
            margin: 0;
            padding: 2rem;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .container {
            max-width: 900px;
            width: 100%;
            position: relative;
        }

        .nav {
            margin-bottom: 3rem;
        }

        .back-link {
            color: var(--muted);
            text-decoration: none;
            font-size: 0.9rem;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            transition: color 0.2s;
        }

        .back-link:hover {
            color: var(--text);
        }

        .header {
            text-align: center;
            margin-bottom: 4rem;
        }

        .header-subtitle {
            font-family: 'Outfit', sans-serif;
            font-size: 0.75rem;
            font-weight: 800;
            letter-spacing: 0.3em;
            color: var(--muted);
            text-transform: uppercase;
        }

        .title {
            font-family: 'Outfit', sans-serif;
            font-size: 3.5rem;
            font-weight: 800;
            margin: 1rem 0;
            letter-spacing: -0.02em;
        }

        .verdict-badge {
            display: inline-block;
            padding: 0.5rem 1.5rem;
            border-radius: 99px;
            font-size: 0.85rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            background: var(--accent-glow);
            color: var(--accent);
            border: 1px solid var(--accent);
            box-shadow: 0 0 20px var(--accent-glow);
        }

        .section-title {
            font-family: 'Outfit', sans-serif;
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .section-title::after {
            content: "";
            flex: 1;
            height: 1px;
            background: var(--border);
        }

        .rationale-card {
            background: var(--card);
            backdrop-filter: blur(12px);
            padding: 2.5rem;
            border-radius: 20px;
            border: 1px solid var(--border);
            margin-bottom: 4rem;
            font-size: 1.15rem;
            color: #d1d1d1;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            white-space: pre-wrap;
            text-align: justify;
            hyphens: auto;
        }

        .ledger {
            display: grid;
            gap: 1rem;
        }

        .headline-card {
            background: var(--card);
            backdrop-filter: blur(8px);
            padding: 1.5rem;
            border-radius: 12px;
            border: 1px solid var(--border);
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
            transition: transform 0.2s, border-color 0.2s;
        }

        .headline-card:hover {
            transform: translateY(-2px);
            border-color: rgba(255, 255, 255, 0.2);
        }

        .headline-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: var(--muted);
        }

        .source-tag {
            background: rgba(255, 255, 255, 0.05);
            padding: 0.2rem 0.5rem;
            border-radius: 4px;
        }

        .headline-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--text);
            margin: 0;
            font-family: 'Outfit', sans-serif;
        }

        .headline-desc {
            font-size: 0.95rem;
            color: var(--muted);
            margin: 0;
            line-height: 1.6;
        }

        .read-more {
            align-self: flex-start;
            font-size: 0.8rem;
            font-weight: 600;
            color: var(--accent);
            text-decoration: none;
            transition: opacity 0.2s;
        }

        .read-more:hover {
            opacity: 0.8;
        }

        footer {
            margin-top: 6rem;
            text-align: center;
            color: var(--muted);
            font-size: 0.8rem;
            padding-bottom: 4rem;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="nav">
            <a href="/" class="back-link">
                <svg width="15" height="15" viewBox="0 0 15 15" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path
                        d="M6.85355 3.14645C7.04882 3.34171 7.04882 3.65829 6.85355 3.85355L3.70711 7H12.5C12.7761 7 13 7.22386 13 7.5C13 7.77614 12.7761 8 12.5 8H3.70711L6.85355 11.1464C7.04882 11.3417 7.04882 11.6583 6.85355 11.8536C6.65829 12.0488 6.34171 12.0488 6.14645 11.8536L2.14645 7.85355C1.95118 7.65829 1.95118 7.34171 2.14645 7.14645L6.14645 3.14645C6.34171 2.95118 6.65829 2.95118 6.85355 3.14645Z"
                        fill="currentColor" fill-rule="evenodd" clip-rule="evenodd"></path>
                </svg>
                BACK TO OVERVIEW
            </a>
        </div>

        <div class="header">
            <span class="header-subtitle">DAILY ARCHIVE</span>
            <h1 class="title">{{dateStr}}</h1>
            <div class="verdict-badge">{{#if isGood}}Steady{{else}}Arrythmia{{/if}}</div>
        </div>

        <div class="section">
            <h2 class="section-title">Analysis</h2>
            <div class="rationale-card">{{rationale}}</div>
        </div>

        <div class="section">
            <h2 class="section-title">Headlines Ledger</h2>
            <div class="ledger">
                {{#each headlines}}
                <div class="headline-card">
                    <div class="headline-meta">
                        <span class="source-tag">{{this.source.name}}</span>
                    </div>
                    <h3 class="headline-title">{{this.title}}</h3>
                    {{#if this.description}}<p class="headline-desc">{{this.description}}</p>{{/if}}
                    <a href="{{this.url}}" target="_blank" class="read-more">Read Full Story →</a>
                </div>
                {{/each}}
            </div>
        </div>

        <footer>
            THE PULSE &copy; {{year}} | Human collective status record
        </footer>
    </div>
</body>

</html>